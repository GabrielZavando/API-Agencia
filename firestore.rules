rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas de desarrollo: acceso abierto temporal
    // TODO: Restringir antes de producción
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 3, 27);
    }
  }
}
